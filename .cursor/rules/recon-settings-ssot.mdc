---
description: Settings SSOT — .env / .env.example e Settings.from_env
alwaysApply: true
---

# Settings SSOT

- Runtime via `.env`; catalogo em `.env.example` (sem segredos)
- Loader: `infrastructure/config/settings.py`
- Vars `RECON_*`; testes com `RECON_DISABLE_DOTENV=1`
- Doc: `docs/engineering-settings-ssot.md`
- Skill: `recon-settings-change`
