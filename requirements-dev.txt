# SUPABASE-RECON - DEPENDÊNCIAS DE DESENVOLVIMENTO
#
# PYSEC-2022-42969: interrogate puxa o pacote "py" (sem fix no PyPI). O pip-audit em
# requirements-dev.txt usa --ignore-vuln só para esse ID; ver docs/seguranca-dependencias.md.

# === Desenvolvimento e Lint ===
isort>=6.0.0                 # Padronização e organização de imports
ruff>=0.9.0                  # PEP 8, segurança, formato e docstrings (regras D / Google)
interrogate>=1.7.0           # Cobertura mínima de docstrings (complementa o Ruff)
pre-commit>=3.7.0            # Hooks locais (lint + testes + commitlint)
bandit>=1.8.0                # Análise estática de segurança do código
pip-audit>=2.8.0             # Vulnerabilidades conhecidas nas dependências

# === Testes ===
pytest>=8.3.0                # Suite unitária e smoke tests
