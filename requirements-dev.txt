# AETHER ENGINE - DEPENDÊNCIAS DE DESENVOLVIMENTO (V33.0)

# === Desenvolvimento e Lint ===
isort>=6.0.0                 # Padronização e organização de imports
ruff>=0.9.0                  # Auditoria de código ultra-rápida (PEP 8 + segurança)
interrogate>=1.7.0           # Auditoria de cobertura de docstrings
pre-commit>=3.7.0            # Hooks locais (clean workspace + commitlint)
bandit>=1.8.0                # Análise estática de segurança do código
pip-audit>=2.8.0             # Auditoria de vulnerabilidades em dependências
coverage>=7.13.0             # Métrica de cobertura (pre-commit/CI)

# === Testes ===
pytest-asyncio>=0.24.0       # Extensão para testes assíncronos (async/await)
pytest>=8.3.0                # Framework principal de testes unitários
